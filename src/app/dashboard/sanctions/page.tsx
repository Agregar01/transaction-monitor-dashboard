import ComingSoon from "@/components/ComingSoon";

export default function SanctionsPage() {
  return (
    <ComingSoon
      title="Sanctions Screening"
      description="Screen a name (and optional ID number) against all enabled sanctions lists. Returns CLEAR / REVIEW / MATCH."
      phase="Phase 5"
    />
  );
}
