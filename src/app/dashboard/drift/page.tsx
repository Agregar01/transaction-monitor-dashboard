import ComingSoon from "@/components/ComingSoon";

export default function DriftPage() {
  return (
    <ComingSoon
      title="Drift Monitoring"
      description="KS test + PSI per feature, flagged when drift is detected against the reference distribution."
      phase="Phase 6 (backend endpoint pending)"
    />
  );
}
