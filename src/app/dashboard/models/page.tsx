import ComingSoon from "@/components/ComingSoon";

export default function ModelsPage() {
  return (
    <ComingSoon
      title="ML Models"
      description="Model registry — CatBoost, LightGBM, XGBoost, River ARF, Isolation Forest. Champion/challenger lifecycle."
      phase="Phase 6 (backend endpoint pending)"
    />
  );
}
