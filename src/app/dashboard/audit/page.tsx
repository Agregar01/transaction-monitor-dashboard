import ComingSoon from "@/components/ComingSoon";

export default function AuditPage() {
  return (
    <ComingSoon
      title="Audit Trail"
      description="Append-only change log: CREATE / UPDATE / DELETE / APPROVE / REJECT / LOGIN / LOGOUT events with before/after diff."
      phase="Phase 7"
    />
  );
}
