import ComingSoon from "@/components/ComingSoon";

export default function UsersPage() {
  return (
    <ComingSoon
      title="Users & Roles"
      description="Read-only user directory and assigned roles. User creation stays in the CLI/API for v1."
      phase="Phase 7"
    />
  );
}
